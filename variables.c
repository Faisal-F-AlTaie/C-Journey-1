#include <stdio.h>
#include <stdlib.h>

int main() {
    
    char characterName[] = "Tim";
    int characterAge = 77;
    printf("There once was a old man named %s \n", characterName);
    printf("he was %d years old \n", characterAge);
    printf("he really liked the name %s \n", characterName);
    printf("but didn't like being %d. \n", characterAge);

    return 0;
}